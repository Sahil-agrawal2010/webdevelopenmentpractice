// java script is scripting language used to creat dynamic wesites. Itwas created by brendaneiche in 1995 A.D.. It runs in the client site(In the browser). It can also be run on the server sute using node js. Java script can be used by three ways inline,internal,exterrnal. We use script tag to run javascript in html file. It is prototype based object oriented language, which means it doesn't have calsses, Rather it defines behaviors using a constructor funtion and then reuses it using the prototype.